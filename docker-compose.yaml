version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: pr_helper
      POSTGRES_USER: pr_user
      POSTGRES_PASSWORD: pr_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: